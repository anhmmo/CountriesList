import React, { Component } from 'react';

class SelectAll extends Component {
    render() {
        return (
            <div id="select-all" className="select-all">Check all items</div>
        );
    }
}

export default SelectAll;